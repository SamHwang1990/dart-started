/**
 * Enum，枚举
 * 
 * 创建枚举：
 *    `enum Color { red, green, blue }`
 * 
 * 使用枚举：
 *    `Color.red`，获取枚举值
 *    `Color.red.index`，枚举值持有 `index` 的索引，用来指明在 enum 中的位置
 * 
 * switch-case 枚举时，you’ll get a warning if you don’t handle all of the enum’s values:
 * 
 * Enum 中的限制：
 *    - 不允许 extend、with、implement 一个 Enum
 *    - Enum 不允许实例化
 * 
 */

